/// Defines how the outline panel is triggered to open.
enum OutlineTriggerMode {
  /// Panel opens on hover (desktop/web).
  hover,

  /// Panel opens on tap (mobile/tablet).
  tap,

  /// Panel responds to both hover and tap.
  both,
}
